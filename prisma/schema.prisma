generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Link {
  id          String   @id
  urlOriginal String
  cliques     Int      @default(0)
  criadoEm    DateTime @default(now())
}
